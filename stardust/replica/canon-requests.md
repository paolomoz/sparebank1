
## faq worker — 2026-09-14
- author.mjs footer(): `cs.querySelector('.text-container p')` matches a `<p>` inside the hidden `.contact-info-section` panels when the page's contact-section has no top-level text (bedrift kundeservice pages) → an extra visible paragraph (+56px). Should be `cs.querySelector(':scope > .aem-component-container.text-container p')`. Worked around in css/faq.css (`.page--faq .contact__text{display:none}`).

## utility worker — 2026-09-14
- canon `h3` uses var(--font-heading) (SpareBank1-title-medium); live h3 renders SpareBank1-medium 28/36 (lift kontakt-1440: `main h3`). Overridden in css/utility.css for .page--utility.
- canon `tip` module stub (author.mjs) does not match the live `ffe-message-box--info` structure (icon circle 64px + tinted #d8e9f2 box, padding 40/32, radius 8, col-lg-6 centred, section margin 72/48, padding 0 20). Replaced by a `tip` handler in modules/utility.mjs (global registry override — other families get the new markup; CSS for it lives in utility.css, so they should copy the `.tip*` rules or a canon owner should move them into canon.css).
- canon grid lacks `.grid-row--cols-9` (75 % / 960px centred row) — added in utility.css.
- canon `.main > .cols > .grid-row { max-width: var(--container) }` (3 classes) outranks `.grid-row--cols-N { max-width }` (1 class) — the narrowed centred rows never apply on top-level columns-grids. Restated at higher specificity in utility.css; canon should use `.main > .cols > .grid-row--cols-6/8/9`.
- canon `.main > .band:first-of-type { margin-top: -36px }` also fires when the first band is NOT the first main child (kontakt: cols → band). Should be `.breadcrumb + .band` only. Overridden in utility.css.
## from news-article worker (2026-09-14)
- canon.css `body { font: 400 16px/24px var(--font); … }` resets `font-variant-numeric`; the live `body.ffe-body-text` sets
  `font-variant-numeric: tabular-nums` and every element inherits it. Only `p` gets it back in canon. Digit-heavy `li`/headings
  wrap differently (measured: an infobox `li` 9 lines vs live 8 at 360). Suggest `body { font-variant-numeric: tabular-nums; }`
  in canon (after the `font` shorthand).
- (category-hub worker, 2026-09-14) **Registry key collision:** `modules/utility.mjs` and `modules/category-hub.mjs` both export a `tip` handler; author.mjs's `Object.assign` registry makes the alphabetically-last file win for every page. category-hub.css now styles utility.mjs's tip markup (`.tip--small > .tip__col > .tip__box > .tip__icon + .tip__body`). Suggest author.mjs namespaces handlers by `ctx.family` (registry[family][key] before registry.shared[key]) or the orchestrator merges the two `tip` handlers into one shared file.
- (category-hub worker) Mobile menu on live adds `hide` to `main`, `.bank-choice` and `footer` when the hamburger opens (motion lan-360.json) — canon.js only toggles `header__wrap--active`.
- (category-hub worker) `.card--medium` without an image keeps canon's 200px first grid row; live collapses the row (`:not(:has(img))`). Worked around per family (`.page--kundeservice-hub .card--medium:not(:has(.card__media)) { grid-template-rows: 1fr }`) — belongs in canon.
- (category-hub worker) Live `hr.aem-hr` is full-width (1440) at top level on hub pages; canon `.main > .rule` caps it at 1280 (invisible unless `show-hr`). Overridden per family.

## tool worker — 2026-09-14
- author.mjs KEEP_CLASS drops `main-lead` (page lead span: 24/32 centred fjell desktop, 18/24 mobile, margin-bottom 24/18) — add to KEEP_CLASS + canon rule.
- canon `title` module has no geometry (live: container 1280 / padding 20, h1 centred, margin 16 0 24 desktop, 16 0 8 mobile) — rules in tool.css.
- richtext `ol` numbering (`.text-wrapper ol`: list-style none, counter, li margin 16 0 0 32 relative, `::before` counter at left -1.8em top .05em 1.1em fjell) missing from canon — rules in tool.css.
- module gaps: `.main > .text + .columns-grid` and `.main > .tip (+ .hr) + .columns-grid` have margin-top 72 (≥1024) / 48 — rules in tool.css.
- the ffe-message-box `.tip*` CSS is duplicated in utility.css and tool.css — move to canon.css once the `tip` handler is promoted.
- content-diff note: live pages carry a FAQPage JSON-LD <script> inside <main> that the inventory counts as body text (🟡) — justified drop on every FAQ page.
- (category-hub worker, chrome-parity on lan 1440) header top-nav inactive links: canon gives every `.header__topnav a` a 2px transparent bottom border (height 19) — live inactive links have no border (17). Footer social block on hub pages: live LinkedIn/YouTube icons at x 889/938, y 6198 (40px anchors 30×40 / 35×40); canon renders them 35px right and 28px higher with a 50px to-top icon (live 40) — check `.footer__links--social` and `.to-top__icon svg` sizes.

## market-landing worker — 2026-09-14
- author.mjs emits header / bank-choice / main / footer only; the live body also has a `.market-nav` strip ("Privat" · "Gå til bedrift", flex space-between, padding 12 20, 52px, mobile-only) between `</header>` and `.bank-choice` on the three market landings. Inserted verbatim from modules/market-landing.mjs (campaign-carousel handler side effect into ctx.O body, before `.bank-choice`) + styled in market-landing.css — should be emitted natively (and its breakpoint lifted; assumed <1024).
- canon `card()` drops `.card__date` (news cards: block, padding 4 0, 14px/1.4 under the title, +28px) and the `<img class="arrow-right">` chevron in `.card__container-content-arrow` (canon only carries `<svg>` arrows) → 2 images + 4 dates missing on privat.html. Restored via a `related-topics` override in modules/market-landing.mjs, scoped to `ctx.family === 'market-landing'` (other families get canon's byte-identical output).
- canon has no two-column `banner-small` (`.banner-small__columns` → two 720px halves, bg-color2 frost-30 / bg-color4 sand-30, 190px, content 600 row-reverse: text 400 + image 200; mobile stacked, image −60/−90 overlap, bottom column-reverse with logo). Handler `banner-small` overridden in modules/market-landing.mjs (single variant reproduced byte-for-byte) + market-landing.css.
- canon `image` handler loses the authored inline `max-width:70px;width:100%` on illustration images (live band columns) — `image` override passes it as `--w`; canon should keep authored max-width.
- `.richtext > :first-child { margin-top: 0 }` differs from live `.text-wrapper` (a leading `<p>` keeps its 16px top margin inside column-grid text blocks: "OM OSS"/"SNARVEIER" columns 56/216px vs canon 40/200). Overridden for `.page--market-landing .band .richtext`.
- Campaign carousel is client-rendered and rotates campaigns between loads (sidecar/prototype: "Se hva du kan spare!" on #F2F2F9; the live captures of the day: "Noe som gnager litt?" on frost) — the hero (400px / 519px) is a permanent ~5 % / ~3.3 % pixel residual on privat.html; content-diff on the same load matched. No carousel controls exist for a single slide (motion-observe: `.campaign-carousel button` not found).

## From worker C (om-oss / presse), 2026-09-14
- **Grid rows with 9, 10 and 11 columns**: live `columns-grid__row--col-9/10/11` = `repeat(N,1fr)` with max-width 960 / 1066.67 / 1173.33 px (≥1024). Canon only defines cols-4/6/8; om-oss.css carries the three (scoped `.page--om-oss`). Promote to canon; also `.main > .cols > .grid-row {max-width}` outranks them (same specificity issue as noted above).
- **`.button-list .button-wrap` bottom margin**: live keeps `margin-bottom: 8px` on the wraps inside a button-list at desktop too (row "Presserom": content 268px, not 260). Canon only has the mobile rule. om-oss.css adds it scoped.
- **Inline images sit on the baseline**: live `<picture><img>` (campaign photo, svg illustration, adviser portraits) are `display:inline` on the baseline, so the containing block is image height + 7px (line-box descender at 16/24). Canon `img { vertical-align: middle }` removes that tail, shifting everything below by 7px per inline image. Either drop the canon rule or expose a `.img--inline` opt-in.
- **Footer social list (1440 and 360)**: live icons start right under the "Sosiale medier" h2 (y = h2 bottom + 15), canon `.footer__links--social ul { padding-top: 11px }` pushes them 11px lower; Linkedin icon box is 30×40 on live (canon 35). Already-listed 30→35 / 40→50 icon sizes confirmed on the om-oss footer. Mobile: live "Sosiale medier" h2 is 130px wide (canon 170).
- **Button hover colours**: live `.ffe-button--secondary:hover` → background vann rgb(0,90,164) with white text (canon `.btn--secondary:hover` uses fjell); live `.ffe-button--action:hover` → rgb(9,81,57) #095139 (canon #005a3c). Both measured by motion-observe hover probes on presse.
- **OM OSS header variant**: identical to the canon header (topnav OM OSS active, six main-nav links, Søk + Bli kunde + Logg inn); nothing variant-specific — do not hide the login button on om-oss pages.

## lead (product → EDS conversion) — 2026-09-14
- canon.css `.footer__links a` / `.footer__small a` say `text-decoration: none`; the live footer renders every column link and small-print link underlined at 1440 and 360 (live capture, footer crops). The replica footer crops still pass (thin underlines) but the EDS footer mirrors live (underlined). Fix canon to `text-decoration: underline`.
- Live pseudo-headings (`<p><span class="h4">`, `<h2><span class="h3">` — 252 `.h4`, 615 `.h5`, 61 `.h3`, 29 `.h2`, 79 `.h6` spans across the 100 pages) carry their size in a class canon.css does cover (`.h4/.h5/.h6`), but `.h2/.h3` span rules are missing — add `.h2 { 36/44 title face }`, `.h3 { 28/36 }` with the mobile sizes. (EDS side: a heading whose text is entirely a pseudo-heading span takes the span's rank — `lib.mjs headingTag()`.)
- Live plain `<h4>` = 22/28 medium (34 occurrences in main), `h4.ffe-h4` = 18/28 (18/24 mobile) — 2017 occurrences, all footer contact panels. canon.css has both; the EDS side styles `h4` 22/28 and scopes 18/24 to `.footer .contact__panel h4`.
- `.to-top`: the live stitched captures never show the button (its scroll handler does not fire on programmatic scrollTo during capture); canon.js and footer.js show it after 130px — a ~0.2–0.4 % permanent pixel residual in every chunk seam of every gate on both regimes. Not a defect on the page (live users see the button); consider suppressing `.to-top` under an instrument flag (`?stardust-capture`) so it stops polluting the bars.
- Header lists: live `<li>` items are inline-block separated by authored whitespace (≈4.4 px at 16 px). JS-built lists (EDS header.js) must insert text nodes between items; the earlier `word-spacing: 4.6px` hack on the `ul` had no effect (no spaces to widen).

## From worker C (theme / borettslag-sameie and markedsnytt-listing), 2026-09-14
- **`referance` drops columns-grids**: on theme and markedsnytt pages the reference block wraps a `.columns-grid` (text + button-list + image / a white background-container). Canon's `case 'referance'` lifts only `.text` nodes → buttons, images and grid geometry are lost. theme.mjs registers a family-gated `referance` (canon path as fallback for other families); promote: if the block contains `.columns-grid`/`.background-container`, run `moduleOf` on its children.
- **`card()` ignores featured cards and `.card__date`**: `card__container--featured` has a contain-fit illustration (`card__container-image--contain img {padding:8px 8px 0; object-fit:contain; height:200/270px}`) and a full richtext body (`.card__container-content > .card__container-content-wrapper > .text` + a sibling `.button`, `align-content: space-between`); newsfeed/related cards carry `<span class="card__date">`. theme.mjs and market-landing.mjs each patch this behind a family gate.
- **Registry collisions are silent**: `related-topics` is registered by market-landing.mjs, `image` by market-landing.mjs, `tip` by utility.mjs; a later-sorting file wins and other families cannot reach their own patch (markedsnytt-listing.mjs sorts before market-landing.mjs, so its `related-topics` date patch is unreachable → the 3 `.card__date` texts are missing on markedsnytt: content-diff 🟡 ×3, Δh −27). Suggest: registry handlers receive `(node, ctx, next)` where `next` is the previously registered handler, or gate by a family *set* (market-landing.mjs: add 'markedsnytt-listing' to the date/`--w` gates).
- **`title` geometry**: live `.title h1` is centred with `margin: 16px 0 24px` (mobile 16px 0 8px) inside a 1280px/20px-gutter container; canon emits `section.intro > h1` with no geometry.
- **`.text` is a formatting context on live**: the first `<p>` keeps its 16px top margin and the wrapper is 620px wide by default (`.text-wrapper.max-width` carries an authored max-width: 900/700/600/586px) and centred. Canon `.richtext > :first-child {margin-top:0}` removes the 16px on every top-level text.
- **Lead / sub-lead classes**: KEEP_CLASS drops `main-lead` (24/32 fjell centred, 18/24 mobile, margin-bottom 1em), `sub-lead-left` / `lead` / `sub-lead` (18/28 desktop, 17/24 mobile, site font even inside an authored `font-family: tahoma` paragraph). theme.mjs re-tags them by text.
- **Module gaps**: live `.main > .background-container` after the lead has margin-top 72/48; `.main > .image` (icon image, `max-width:250px`, inline baseline) 72/48; `.main > .related-topics` after a band is flush (canon adds 72); `.main > .band:first-of-type` fires on the first *band* even when a `.cols` hero precedes it (markedsnytt).
- **Lists**: live `.text-wrapper ul {margin:0 0 16px; padding:0; list-style:disc}` + `li {margin:8px 0 0 16px}`, and at ≥768 `ul {margin-left:16px}` `li {margin-left:32px}`; `.checked-list ul li` is `margin:16px 0 0 32px; position:relative; line-height:1.5em` with a `::before` ✓ data-URL bullet (fjell) at `left:-2em; top:.15em; 1em×1em`.
- **Heading utilities**: `.h3` inside an `h2` renders 28/36 (20/24 mobile); `h3` is the medium face 28/36 (canon: title face → wraps differently, e.g. markedsnytt band 5 row 3 Δ36); `h4` mobile line-height is 28 on live.
- **Buttons**: `.ffe-button--shortcut` = secondary look (white bg, 2px vann border, vann text, hover inverted), single-line inline-flex; canon has `.btn--shortcut` only in category-hub.css.
- **`ffe-message-box--tips` variant of the tip**: icon ring `#dc8000` (sol), svg 40px sol, box background `#f8e9dd` (sand), the box's action button renders as a text button (vann, transparent border, radius 22, padding 8 17, flex-wrap) — utility.mjs emits the structure, the tips colours live in theme.css.
- **BEDRIFT footer contact block**: live keeps an empty text container between "Kontakt oss" and the action row that still reserves 24px (≥768) / 8px (mobile). Canon `.contact__actions {margin-top:64px}` is 24/8 short on pages without contact text.
- **Video module**: `.video-wrap` is a 16:9 padding-bottom box (`padding-bottom:56.25%`, position relative) with an absolutely positioned placeholder `<img>` and `<iframe>`; the lazyloader sets `src` from `data-video-url` on scroll. Canon drops iframes; markedsnytt-listing.mjs mirrors the loaded state.
