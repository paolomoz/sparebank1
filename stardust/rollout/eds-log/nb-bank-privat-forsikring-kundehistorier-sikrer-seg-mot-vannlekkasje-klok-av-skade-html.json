{
 "slug": "nb-bank-privat-forsikring-kundehistorier-sikrer-seg-mot-vannlekkasje-klok-av-skade-html",
 "daPath": "/nb/bank/privat/forsikring/kundehistorier/sikrer-seg-mot-vannlekkasje-klok-av-skade",
 "file": "content/nb/bank/privat/forsikring/kundehistorier/sikrer-seg-mot-vannlekkasje-klok-av-skade.html",
 "chrome": {
  "nav": "/nav",
  "footer": "/footer"
 },
 "modules": [
  {
   "module": "story",
   "blocks": [
    "story"
   ]
  }
 ],
 "blocks": [
  "story"
 ],
 "notes": [
  "story slide: video authored as a link to the .mp4 (its text = the live title \"Bjørn og Wenche sitter rundt kjøkkenbordet og forteller hva som skjedde da de fikk teleonen om lekkasje\"); autoplay/muted/loop/controls + the fallback string are the block's chrome",
  "spacer: blank <p> dropped (live renders it as vertical space)",
  "spacer: blank <p> dropped (live renders it as vertical space)",
  "spacer: blank <p> dropped (live renders it as vertical space)",
  "spacer: blank <p> dropped (live renders it as vertical space)",
  "story: unmodelled live component (sb1-story columns-noColor / columns-bgColor: figure + caption, pseudo-heading text, button group — the replica module carries it as a verbatim copy) emitted as default content; needs a live lift before production",
  "story: unmodelled live component (sb1-story columns-noColor / columns-bgColor: figure + caption, pseudo-heading text, button group — the replica module carries it as a verbatim copy) emitted as default content; needs a live lift before production",
  "spacer: blank <p> dropped (live renders it as vertical space)",
  "spacer: blank <p> dropped (live renders it as vertical space)",
  "spacer: blank <p> dropped (live renders it as vertical space)",
  "spacer: blank <p> dropped (live renders it as vertical space)",
  "story: unmodelled live component (sb1-story columns-noColor / columns-bgColor: figure + caption, pseudo-heading text, button group — the replica module carries it as a verbatim copy) emitted as default content; needs a live lift before production",
  "story slide: a live paragraph is centred (p.ta-center) — David's Model carries no inline alignment; the card text stays left-aligned like the other slides (justified residual)",
  "story: unmodelled live component (sb1-story columns-noColor / columns-bgColor: figure + caption, pseudo-heading text, button group — the replica module carries it as a verbatim copy) emitted as default content; needs a live lift before production",
  "spacer: blank <p> dropped (live renders it as vertical space)",
  "spacer: blank <p> dropped (live renders it as vertical space)",
  "story: unmodelled live component (sb1-story columns-noColor / columns-bgColor: figure + caption, pseudo-heading text, button group — the replica module carries it as a verbatim copy) emitted as default content; needs a live lift before production",
  "template: news-article → campaign-landing (W5 _w5-template-fix.mjs — the live page is an sb1-story; roster misclassification filed in eds-requests.md)"
 ],
 "gaps": [],
 "assets": [],
 "rasterise": [],
 "writtenAt": "2026-09-14T20:43:36.872Z"
}